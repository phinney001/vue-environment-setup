import { defineComponent, onMounted } from 'vue'

// @page HEADERTITLE
const COMPONENT = defineComponent(() => {

  // 初始化加载数据
  onMounted(() => {

  })

  return () => (
    <></>
  )
})

export default COMPONENT
